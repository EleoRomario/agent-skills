{
  "name": "agent-skills",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-manifest.mjs",
    "prebuild": "npm run sync",
    "dev": "npm run sync && npm run dev --prefix catalog",
    "build": "npm run sync && npm run build --prefix catalog",
    "preview": "npm run preview --prefix catalog"
  }
}
